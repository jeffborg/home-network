---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tvheadend-restart
  namespace: media
spec:
  chart:
    spec:
      # renovate: registryUrl=https://decayofmind.github.io/charts/
      chart: rollout-restart
      version: 1.0.3
      sourceRef:
        kind: HelmRepository
        name: decayofmind
        namespace: flux-system
  install: {}
  interval: 10m0s
  values:
    # kubectl
    image:
      repository: "rancher/kubectl"
      tag: "v1.35.2"
    schedule: "0 2 * * *"
    targetResources:
      - deployment/tvheadend
