# kustomization.yml
namespace: default
resources:
  - deployment.yaml
configMapGenerator:
  - name: avahi-services
    files:
      - services/homekit.service
  - name: avahi-config
    files:
      - avahi-daemon.conf
    