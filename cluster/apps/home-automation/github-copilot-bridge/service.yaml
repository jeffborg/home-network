---
apiVersion: v1
kind: Service
metadata:
  name: github-copilot-bridge
  namespace: home-automation
spec:
  type: ClusterIP
  selector:
    app: github-copilot-bridge
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
